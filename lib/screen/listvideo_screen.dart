import 'package:flutter/material.dart';

class ListvideoScreen extends StatelessWidget {
  const ListvideoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('List video screnne');
  }
}
