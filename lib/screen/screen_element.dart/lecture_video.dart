import 'package:flutter/material.dart';

class LectureVideoScreen extends StatelessWidget {
  const LectureVideoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('lecture video');
  }
}
